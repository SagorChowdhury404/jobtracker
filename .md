<!--todo . What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll? -->
<!-- 
getElementById selects a single element by id.

getElementsByClassName selects all element by className

querySelector selects first element matches css selector

querySelectorAll  selects all element matches css selector

 -->



<!-- 
2. How do you create and insert a new element into the DOM? -->

use document.createElement('id');
const newDiv =document.createElement('div')
newDiv.innerHtml=` <h1> hello world </h1>`
newDiv.classList.add('myDivClass')
newDiv.id.add('myDivId')

document.body.appendChild(newDiv)

<!-- 3. What is Event Bubbling? And how does it work? -->
Event Bubbling is when an event  element starts at the target and propagates up to its parent
<!-- 
4. What is Event Delegation in JavaScript? Why is it useful? -->
Event delegation is a JavaScript design pattern where a single event listener is attached to a parent element to manage events for multiple child elements


<!-- 5. What is the difference between preventDefault() and stopPropagation() methods? -->
While both methods are used to manage how events behave, they control entirely different things: preventDefault() stops the browser's built-in actions, while stopPropagation() stops the event's movement through the DOM